# Bank Cards Management System

Система управления банковскими картами: создание, блокировка, просмотр, переводы между картами, управление пользователями.

---

## 🧰 Технологии

- Java 21  
- Spring Boot 3.5.7  
- Spring Security + JWT  
- Spring Data JPA  
- PostgreSQL  
- Liquibase  
- Docker / Docker Compose  
- Swagger / OpenAPI  

---

## ⚙️ Требования

- JDK 21+  
- Maven 3+  
- Docker + Docker Compose  

---

## 🚀 Установка и запуск

### Сборка проекта

Выполнить команду `mvn clean package` для сборки jar.

### Запуск через Docker Compose

docker-compose up --build  

Приложение подхватит переменные окружения:

- `SPRING_DATASOURCE_URL` — URL базы данных  
- `SPRING_DATASOURCE_USERNAME` — имя пользователя БД  
- `SPRING_DATASOURCE_PASSWORD` — пароль БД  

---

## 📦 Структура проекта (основные пакеты)

- `controller` — REST-контроллеры  
- `service` — бизнес-логика  
- `dto` — DTO для API  
- `entity` — JPA-сущности  
- `repository` — Spring Data JPA репозитории  
- `config` — конфигурации Spring, Security, JWT  
- `util` — вспомогательные классы (шифрование, маскирование)  

---

## 📄 API Документация

- Swagger UI доступен по адресу: [http://localhost:8080/swagger-ui/index.html]  
- OpenAPI спецификация хранится в `docs/openapi.yaml`  

---

## 🧪 Тестирование

- Тестовые данные создаются через админский аккаунт  
- API тестируется через Postman или curl  

---

## 🔐 Безопасность

- Ролевой доступ (`ADMIN`, `USER`)  
- Шифрование номеров карт (AES)  
- JWT аутентификация  
- Маскирование номеров карт  

---

## 💡 Особенности

- Пагинация и фильтрация при получении списков карт и пользователей  
- Переводы между картами с проверкой баланса  
- Управление статусами карт: `ACTIVE`, `BLOCKED`, `EXPIRED`  
- Liquibase миграции для работы с БД  

---

## 📤 Сдача проекта

- Git-репозиторий с открытым доступом  
- README.md с инструкцией запуска  
- Swagger / OpenAPI документация  
